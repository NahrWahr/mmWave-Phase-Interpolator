[project]
name = "mmwave-phase-interpolator-plots"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.9",
    "numpy>=2.0",
    "scipy>=1.13",
]

[tool.uv]
dev-dependencies = []
